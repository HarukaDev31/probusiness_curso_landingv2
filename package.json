{
  "name": "probusiness_cursov2",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "optimize:images": "node scripts/optimize-images.mjs",
    "encode:videos": "node scripts/encode-videos.mjs",
    "upload:assets": "node scripts/upload-assets.mjs",
    "deploy:assets": "npm run optimize:images && npm run upload:assets",
    "deploy:videos": "npm run encode:videos && npm run upload:assets"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^6.4.6",
    "tailwindcss": "^4.3.1"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "vite": "^7.3.2"
  },
  "devDependencies": {
    "sharp": "^0.35.1"
  }
}
